-- Put any text you want in the Information panel here, then force-reload
-- the pack (Ctrl+Shift+R). Newlines inside [[...]] are displayed as line breaks.
-- Replace nil below with a string, for example:
-- SetHintText([[Check the well after getting the hammer.
-- Save rupees for the fairy.]])
--
-- SetHintText(nil) restores automatic hints; SetHintText("") clears the panel.
-- You can also call SetHintText from any Lua callback after pack initialization.
-- Custom text survives auto-tracking refreshes and reconnects, but runtime
-- changes are not saved: reloading the pack runs this file again.

-- SetHintText("Hello, World!")
SetHintText(nil)
